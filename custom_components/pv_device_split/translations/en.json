{
  "config": {
    "flow_title": "Solar Load Split: {name}",
    "step": {
      "user": {
        "title": "Solar Load Split",
        "description": "Choose the grid import/export power sensor once. After optional inversion: positive = grid import, negative = grid export. Solar Load Split will use this sensor to discover matching device power sensors.",
        "data": {
          "name": "Name",
          "grid_power": "Grid import/export power sensor",
          "invert_grid": "Invert grid sign"
        }
      },
      "discovery_confirm": {
        "title": "Solar Load Split discovered power sensors",
        "description": "Discovered device: {device_name}\nEntity: {device_entity}\n\nCheck the suggestion and adjust it if needed.",
        "data": {
          "name": "Name",
          "device_power": "Device power sensor"
        }
      }
    },
    "abort": {
      "already_configured": "This device and grid sensor pair is already configured.",
      "discovery_missing": "The discovered sensor information is no longer available."
    }
  },
  "entity": {
    "sensor": {
      "pv_power": {
        "name": "{device_name} PV Power"
      },
      "grid_power": {
        "name": "{device_name} Grid Power"
      },
      "pv_energy": {
        "name": "{device_name} PV Energy"
      },
      "grid_energy": {
        "name": "{device_name} Grid Energy"
      }
    }
  }
}
